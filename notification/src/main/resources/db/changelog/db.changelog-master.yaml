# Liquibase master changelog for notification-db; includes versioned changesets in order.
# 001 creates the processed-events dedupe table.
databaseChangeLog:
  - include:
      file: db/changelog/changes/001-create-processed-events.yaml